# Bold Dragoon CC Hub — Version 12.1 Supabase Ready

Final preparation release before Supabase setup.

Fixed:
- Safari-safe weather rendering using explicit DOM lookups
- Weather can no longer remain on Loading indefinitely
- Clear message if the Netlify weather function folder was not deployed
- Updated service-worker and local-storage cache versions
- Added SUPABASE_NEXT_STEPS.txt with the exact setup order

All non-Play-Cricket app features remain included.
